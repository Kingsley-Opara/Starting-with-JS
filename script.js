const personality = {
    Name: 'Opara Udochukwu',
    Height: '5.6ft',
    Country: 'Nigeria'
};
const {Name, Height, Country} = personality
const result = `I'm ${Name}, ${Height} tall in height and from ${Country}`
console.log(result)
document.write(result)